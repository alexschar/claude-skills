# Ledger — Blockers & Inputs Needed

Rules: one variable per question to the owner, with a concrete example/table, a recommendation,
and the consequence in one sentence. Record the owner's answer VERBATIM, strike the variable
through, and copy the quote to ../INTENT-EVIDENCE.md. Rulings are never retired for being old.

## {{MISSION}} (wargame {{DATE}}) — OPEN inputs
- **(VARIABLE_NAME)** — the question, stated so the owner can answer in one line. Lean: {{what
  recon suggests}}. Needed by Move {{N}}; {{"build proceeds on the lean if unruled" | "blocks the move"}}.

## Resolved
- ~~(EXAMPLE)~~ **RESOLVED {{DATE}} (owner: "exact words"):** what was decided, in one sentence.
